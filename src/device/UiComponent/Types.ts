export type ComponentGeometry = {
    x: number | null,
    y: number | null,
    w: number | null,
    h: number | null,
}

/**
 * @deprecated
 */
export type TouchEventData = {
    x: number,
    y: number,
}
